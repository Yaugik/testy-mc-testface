# Synthetic Hunter package

This package models domain contact discovery using only reserved example domains
and synthetic addresses. It covers found, empty, partial, invalid, suppressed,
quota, authentication, conflicting, timeout and recovery behavior.
