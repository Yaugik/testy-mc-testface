# Synthetic Apollo package

This package models company enrichment through a JSON-body matched operation.
All domains, identifiers and payloads are synthetic and reserved for testing.

Coverage includes complete and partial matches, no result, conflicting and
suppressed results, schema-invalid payloads, quota exhaustion, retryable and
permanent failures, authentication failure, and deterministic recovery.
